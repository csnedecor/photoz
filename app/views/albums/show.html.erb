<h1><%= @album.name %></h1>
<p>
  <%= @album.description %>
</p>
<div id = "user">
  <%= @album.user.username %>
</div>

<% n = 0 %>
<% @album.photos.each do |photo| %>
  <div id= "photo<%= n %>"><%= image_tag(photo.photo.url) %></div>
  <% n += 1 %>
<% end %>
